# ── MCP Server ────────────────────────────────────────────────────────────────
# The browser-side URL the app uses to reach the MCP server.
# Defaults to /mcp which is proxied by Next.js (see next.config.ts rewrites)
# to avoid CORS issues. You generally don't need to change this.
NEXT_PUBLIC_MCP_SERVER_URL=/mcp

# The actual MCP server URL (server-side only, used by the Next.js proxy).
# Point this at your running MCP server (created via `stellar-mcp generate`).
MCP_PROXY_TARGET=http://localhost:3000/mcp

# ── Stellar Network ──────────────────────────────────────────────────────────
NEXT_PUBLIC_RPC_URL=https://soroban-testnet.stellar.org
NEXT_PUBLIC_NETWORK_PASSPHRASE="Test SDF Network ; September 2015"

# ── PasskeyKit ────────────────────────────────────────────────────────────────
# Smart wallet WASM hash — deploy once per network, reuse for all wallets.
# This testnet hash was built from https://github.com/kalepail/passkey-kit (next branch).
# To deploy your own: cd contracts && make build && make upload
NEXT_PUBLIC_WALLET_WASM_HASH=b3eba29c256a2458faff84fc872226d29df37ad05a6a1c0efe2a95817d6765ec

# ── OpenZeppelin Relayer (fee sponsorship) ────────────────────────────────────
# OZ Relayer sponsors transaction fees so users need no XLM.
# Sponsors transaction fees so users need no XLM.
# Get a testnet API key: https://channels.openzeppelin.com/testnet/gen
NEXT_PUBLIC_RELAYER_URL=https://channels.openzeppelin.com/testnet
NEXT_PUBLIC_RELAYER_API_KEY=your-oz-relayer-api-key-here