# URL of your running MCP server (created with `stellar-mcp generate`)
MCP_SERVER_URL=http://localhost:3000/mcp

# Stellar network
RPC_URL=https://soroban-testnet.stellar.org
NETWORK_PASSPHRASE="Test SDF Network ; September 2015"

# Secret key for signing write operations (starts with S...)
# If not set, write ops show the XDR preview but do not submit
SIGNER_SECRET=
