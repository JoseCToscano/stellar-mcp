# MCP_URL is intentionally omitted — the app uses window.location.origin + '/mcp'
# so requests go through the Vite proxy (see vite.config.ts) and avoid CORS issues.
# Uncomment to point directly at the server (must have CORS headers):
# VITE_MCP_URL=http://localhost:3001/mcp

VITE_RPC_URL=https://soroban-testnet.stellar.org
VITE_NETWORK_PASSPHRASE=Test SDF Network ; September 2015
