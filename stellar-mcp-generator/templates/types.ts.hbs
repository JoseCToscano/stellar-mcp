// Generated TypeScript types for {{contract_name}}

{{#each types}}
{{#if doc}}
/** {{doc}} */
{{/if}}
{{#if is_struct}}
export interface {{name_pascal}} {
  {{#each fields}}
  {{#if doc}}
  /** {{doc}} */
  {{/if}}
  {{name_camel}}: {{ts_type}};
  {{/each}}
}

{{/if}}
{{#if is_enum}}
export enum {{name_pascal}} {
  {{#each variants}}
  {{#if doc}}
  /** {{doc}} */
  {{/if}}
  {{name}} = {{value}},
  {{/each}}
}

{{/if}}
{{#if is_union}}
export type {{name_pascal}} =
  {{#each cases}}
  | { type: '{{name}}'{{#if has_value}}; value: {{ts_type}}{{/if}} }{{#if @last}};{{/if}}
  {{/each}}

{{/if}}
{{/each}}
