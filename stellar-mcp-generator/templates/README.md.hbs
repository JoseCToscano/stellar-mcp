# {{server_name_pascal}} MCP Server

Auto-generated MCP server for the {{contract_name}} contract on Stellar {{network_name}}.

## Contract Information

- **Contract ID**: `{{contract_id}}`
- **Network**: {{network_name}}
- **RPC URL**: {{rpc_url}}

## Requirements

- **Node.js 20+** recommended
- For Node.js 18.x users experiencing SSL certificate errors, add `NODE_TLS_REJECT_UNAUTHORIZED=0` to environment variables (development only)

## Available Tools

{{#each functions}}
### `{{name_kebab}}`

{{doc}}

{{#if has_inputs}}
**Parameters:**

{{#each inputs}}
- `{{name}}` ({{ts_type}}): {{doc}}
{{/each}}
{{/if}}

{{#if has_output}}
**Returns:** `{{output_type}}`
{{/if}}

{{/each}}

## Quick Start

```bash
# Install dependencies
npm install

# Configure environment
cp .env.example .env
# Edit .env with your credentials

# Build
npm run build

# Start server
npm start
```

## Claude Desktop Configuration

Add to `~/Library/Application Support/Claude/claude_desktop_config.json`:

```json
{
  "mcpServers": {
    "{{contract_name}}": {
      "command": "node",
      "args": ["/absolute/path/to/{{output_dir}}/dist/index.js"],
      "env": {
        "CONTRACT_ID": "{{contract_id}}",
        "RPC_URL": "{{rpc_url}}",
        "NETWORK_PASSPHRASE": "{{network_passphrase}}"
      }
    }
  }
}
```

**Notes:**
- Replace `/absolute/path/to/` with the actual full path to this directory
- If using Node.js 18.x and encountering SSL certificate errors, add `"NODE_TLS_REJECT_UNAUTHORIZED": "0"` to the `env` object (development only)

## Generated by

[stellar-mcp-generator](https://github.com/stellar/stellar-mcp-generator)
